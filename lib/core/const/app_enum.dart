enum CustomTextFieldType {
  text,
  yearPicker,
  datePicker,
  dropdown,
  money,
  textArea,
}

enum AlertType {
  success,
  error,
  warning,
}

enum SortType {
  newest,
  oldest,
}

enum TextTransformType {
  normal,
  uppercase,
  lowercase,
  capitalize,
  capitalizeWords,
}

enum Results {
  success,
  error,
  wraning,
}
